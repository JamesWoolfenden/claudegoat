name: ci
on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      # CASE: mutable action ref — tag not pinned to commit SHA
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
      - run: echo ok
